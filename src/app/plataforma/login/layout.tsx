import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Plataforma",
};

export default function PlataformaLoginLayout({ children }: { children: React.ReactNode }) {
  return children;
}
